#include <stdio.h>
int main() {
    int num;

    printf("Enter the number");

    scanf("%d",&num);
    if(num%5==0){
        printf("Divisible by 5");
    }
    else{
        printf("Not Divisible");
    }
    return 0;
}
