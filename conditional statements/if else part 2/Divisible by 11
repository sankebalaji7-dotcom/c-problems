#include <stdio.h>
int main() {
    int num;
    
    printf("Enter the number:");
    scanf("%d",&num);

    if(num %11==0){

        printf("divisible by 11");
    }
    else{
        printf("not divisible");
    }
    
    return 0;
}
