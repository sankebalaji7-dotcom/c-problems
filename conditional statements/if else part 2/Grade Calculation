#include <stdio.h>
int main() {
    int marks;

    printf("Enter the marks:");
    scanf("%d",&marks);

    if(marks>=75 && marks<=90){
        printf("B Grade");
    }
    else{
        printf("C Grade");
    }
    
    return 0;
}
