#include <stdio.h>
int main() {
    int a,b;

    scanf("%d %d",&a,&b);

    if(a==b){
        printf("Equal");
    }
    else if(a==a){
        printf("Not Equal");
    }
    else{
        printf("Equal");
    }
    
    return 0;
}
