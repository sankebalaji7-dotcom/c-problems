#include <stdio.h>
int main() {
    int age,salary;
    scanf("%d",&age);
    scanf("%d",&salary);
    if(age>=25)
    if(salary>=30000)
    {
        printf("ealigble");
    }
    else{
        printf("not ealigble");
    }
    else{
        printf("age is less");
    }
    return 0;
}