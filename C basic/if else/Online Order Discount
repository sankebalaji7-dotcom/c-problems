#include <stdio.h>

int main() {
    int amount;

    scanf("%d", &amount);

    if (amount >= 1000) {
        printf("Discount\n");
    } 
    else {
        printf("No Discount\n");
    }

    return 0;
}
