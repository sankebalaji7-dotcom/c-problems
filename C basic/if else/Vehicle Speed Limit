#include <stdio.h>
int main() {
    int speed;

    scanf("%d",&speed);
    
    if(speed>=90){
        printf("speed limit exceeded");
    }
    else{
        printf("speed limit");
    }
    
    return 0;
}
