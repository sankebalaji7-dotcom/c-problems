#include <stdio.h>
int main() {
    int withdrawal;

    scanf("%d",&withdrawal);

    if(withdrawal>500){
        printf("Valid amount");
    }
    else{
        printf("Invalid amount");
    }
    return 0;
}
